labels:
{{ .Values.labels | indent(.Values.indent_value) | squote }}
{{ .Values.labels | quote | indent(6) }}
{{ .Values.labels | toYaml | indent(4) }}

somethingElse:
  {{ hello }}

car:
  {{ .Values.some_value | toYaml('.') | quote | indent(8) }}

ref:
  {{ .Chart.AppVersion }}